#### Solution: Preparing for Nimble

```{r}
## environment for passing to Nimble
forNimble <- list(
  y=y2,
  x=x,
  N = length(y2))
```

```{r}
## initial values, as list
inits <- list(
  beta=c(0,0),
  sigma=5
  )

```

Your BUGS code should look something like this:
```
```{r echo=FALSE, results = 'asis'}
cat(paste(readLines('../../solution_bug/regression2.bug'),collapse="\n"))
```
```

